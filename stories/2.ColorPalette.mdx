import {
  Anchor,
  ColorItem,
  ColorPalette,
  Controls,
  Meta,
  Story,
  Subheading,
  Subtitle,
  Title,
  Unstyled,
} from "@storybook/addon-docs/blocks";
import { Translation } from "react-i18next";

export const CONTROLS = ["Info", "Danger", "Success", "Warning"];
export const COLORS = [
  "Scarlet",
  "Pumpkin",
  "Marigold",
  "Amber",
  "Lime",
  "Jade",
  "Avocado",
  "Teal",
  "Azure",
  "Denim",
  "Iris",
  "Lilac",
  "Orchid",
  "Coral",
  "Wood",
  "Terracotta",
];
export const TINTS = ["Silver", "Steel", "Olive", "Blush", "Sand", "Slate"];

<Meta title="Color Palette" />

<Title>Color Palette</Title>

<Unstyled>
  <div>
    Color palette is curated using the CSS `color-mix` function, unlike other
    palettes this palette uses background and text colors to create shades.
  </div>
  <div>
    From experience shades are not constant between light and dark modes, hence
    the palette allows for switch of shades without having to use `dark:`
    variant.
  </div>
</Unstyled>
> 50 - 400 mixes background color. 500 - base color. 600 - 950 mixes text color.

<hr />

<Subheading>Application Colors</Subheading>
<ColorPalette>
  <ColorItem
    title="Background"
    colors={{
      content: `var(--bg-color-content)`,
      default: `var(--bg-color-default)`,
      dimmed: `var(--bg-color-dimmed)`,
      muted: `var(--bg-color-muted)`,
      gray: `var(--bg-color-gray)`,
      dark: `var(--bg-color-dark)`,
      light: `var(--bg-color-light)`,
      invert: `var(--bg-color-invert)`,
    }}
  />
  <ColorItem
    title="Text"
    colors={{
      default: `var(--text-color-default)`,
      dimmed: `var(--text-color-dimmed)`,
      muted: `var(--text-color-muted)`,
      gray: `var(--text-color-gray)`,
      dark: `var(--text-color-dark)`,
      light: `var(--text-color-light)`,
      invert: `var(--text-color-invert)`,
    }}
  />
  <ColorItem
    title="Border"
    colors={{
      default: `var(--border-color-default)`,
      bright: `var(--border-color-bright)`,
      soft: `var(--border-color-soft)`,
      gray: `var(--border-color-gray)`,
      dark: `var(--border-color-dark)`,
      light: `var(--border-color-light)`,
      invert: `var(--border-color-invert)`,
    }}
  />
</ColorPalette>

<Subheading>Control Colors</Subheading>
<ColorPalette>
  {CONTROLS.map((c) => (
    <ColorItem
      key={c}
      title={c}
      colors={{
        50: `var(--color-${c.toLowerCase()}-50)`,
        100: `var(--color-${c.toLowerCase()}-100)`,
        200: `var(--color-${c.toLowerCase()}-200)`,
        300: `var(--color-${c.toLowerCase()}-300)`,
        400: `var(--color-${c.toLowerCase()}-400)`,
        500: `var(--color-${c.toLowerCase()}-500)`,
        600: `var(--color-${c.toLowerCase()}-600)`,
        700: `var(--color-${c.toLowerCase()}-700)`,
        800: `var(--color-${c.toLowerCase()}-800)`,
        900: `var(--color-${c.toLowerCase()}-900)`,
        950: `var(--color-${c.toLowerCase()}-950)`,
      }}
    />
  ))}
</ColorPalette>

<Subheading>Palette Colors</Subheading>
<ColorPalette>
  {COLORS.map((c) => (
    <ColorItem
      key={c}
      title={c}
      colors={{
        50: `var(--color-${c.toLowerCase()}-50)`,
        100: `var(--color-${c.toLowerCase()}-100)`,
        200: `var(--color-${c.toLowerCase()}-200)`,
        300: `var(--color-${c.toLowerCase()}-300)`,
        400: `var(--color-${c.toLowerCase()}-400)`,
        500: `var(--color-${c.toLowerCase()}-500)`,
        600: `var(--color-${c.toLowerCase()}-600)`,
        700: `var(--color-${c.toLowerCase()}-700)`,
        800: `var(--color-${c.toLowerCase()}-800)`,
        900: `var(--color-${c.toLowerCase()}-900)`,
        950: `var(--color-${c.toLowerCase()}-950)`,
      }}
    />
  ))}
</ColorPalette>

<Subheading>Tint Colors</Subheading>
<ColorPalette>
  {TINTS.map((c) => (
    <ColorItem
      key={c}
      title={c}
      colors={{
        50: `var(--color-${c.toLowerCase()}-50)`,
        100: `var(--color-${c.toLowerCase()}-100)`,
        200: `var(--color-${c.toLowerCase()}-200)`,
        300: `var(--color-${c.toLowerCase()}-300)`,
        400: `var(--color-${c.toLowerCase()}-400)`,
        500: `var(--color-${c.toLowerCase()}-500)`,
        600: `var(--color-${c.toLowerCase()}-600)`,
        700: `var(--color-${c.toLowerCase()}-700)`,
        800: `var(--color-${c.toLowerCase()}-800)`,
        900: `var(--color-${c.toLowerCase()}-900)`,
        950: `var(--color-${c.toLowerCase()}-950)`,
      }}
    />
  ))}
</ColorPalette>
