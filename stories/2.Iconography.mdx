import { Meta } from "@storybook/addon-docs";
import {
  Anchor,
  IconGallery,
  IconItem,
  Story,
  Subheading,
  Subtitle,
  Title,
  Unstyled,
} from "@storybook/blocks";
import { Translation } from "react-i18next";

import { CoreIcons, Icon } from "../packages/core/src";

<Meta title="Iconography" />

<Title>Iconography</Title>

<IconGallery>
  {Object.entries(CoreIcons)
    .sort()
    .map(([key, icon]) => (
      <IconItem key={key} name={key}>
        <Icon
          icon={icon}
          onClick={() => {
            navigator.clipboard.writeText(`CoreIcons.${key}`);
          }}
        />
      </IconItem>
    ))}
</IconGallery>
