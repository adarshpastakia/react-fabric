import { Collapsable } from "@react-fabric/core";
import {
  Canvas,
  Description,
  Primary,
  Story,
  Title,
} from "@storybook/addon-docs";
import {
  Anchor,
  ArgTypes,
  Controls,
  Meta,
  Subheading,
} from "@storybook/blocks";
import { Fragment } from "react";
import { Form } from "../../src";
import * as FormStories from "./Form.stories";
import * as ArrayInputStories from "./ArrayInput.stories";

<Meta of={FormStories} />

<Title>Form</Title>

<Anchor storyId="form">
  <div className="hidden-anchor">
    <Subheading>Form</Subheading>
  </div>
</Anchor>

<Description of={Form} />

<Primary />

<Collapsable className="control-panel">
  <div>Controls</div>
  <Controls of={FormStories._Form} />
</Collapsable>

<Anchor storyId="array">
  <Subheading>Array Input</Subheading>
</Anchor>
<Description of={ArrayInputStories._ArrayInput} />
<Canvas withToolbar of={ArrayInputStories._ArrayInput} />
<Collapsable className="control-panel">
  <div>Controls</div>
  <Controls of={ArrayInputStories._ArrayInput} />
</Collapsable>

<hr />

<Anchor storyId="props">
  <Subheading>Props</Subheading>
</Anchor>

<ArgTypes sort="requiredFirst" />

<Anchor storyId="sample">
  <Subheading>Example</Subheading>
</Anchor>

```tsx
import { Form } from "@react-fabric/form";

export const Example = () => {
  return (
    <Form
      schema={yup schema}
      defaultValue={{}}
      onSubmit={}
      onChange={}
    />
  );
};
```
