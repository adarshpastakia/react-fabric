import { Collapsable } from "@react-fabric/core";
import {
  Canvas,
  Description,
  Primary,
  Story,
  Title,
} from "@storybook/addon-docs";
import {
  Anchor,
  ArgTypes,
  Controls,
  Meta,
  Subheading,
} from "@storybook/addon-docs";
import { Fragment } from "react";
import { DateDisplay, DatePanel } from "../../src";
import * as DateDisplayStories from "./DateDisplay.stories";
import * as DateDurationStories from "./DateDuration.stories";

<Meta of={DateDisplayStories} />

<Title>Date Display</Title>

<Anchor storyId="datepanel">
  <div className="hidden-anchor">
    <Subheading>Date Display</Subheading>
  </div>
</Anchor>

<Description of={DateDisplay} />

<Primary />

<Collapsable className="control-panel">
  <div>Controls</div>
  <Controls of={DateDisplayStories._DateDisplay} />
</Collapsable>

<Anchor storyId="rangepanel">
  <Subheading>Date Distance</Subheading>
</Anchor>
<Description of={DateDurationStories._DateDuration} />
<Canvas withToolbar of={DateDurationStories._DateDuration} />
<Collapsable className="control-panel">
  <div>Controls</div>
  <Controls of={DateDurationStories._DateDuration} />
</Collapsable>

<hr />

<Anchor storyId="props">
  <Subheading>Props</Subheading>
</Anchor>

<ArgTypes sort="requiredFirst" />

<Anchor storyId="sample">
  <Subheading>Example</Subheading>
</Anchor>

```tsx
import { DateDisplay } from "@react-fabric/date";

export const Example = () => {
  return <DateDisplay date="" format="" />;
};
```
